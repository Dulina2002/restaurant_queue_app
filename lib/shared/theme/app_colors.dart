import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary & Accents
  static const Color primary = Color(0xFF0D3B2E);       // Forest Dark Green
  static const Color primaryLight = Color(0xFF165B47);  // Lighter Forest Green
  static const Color accentOrange = Color(0xFFFF6B4A);  // Coral / Orange (Turnover & Peak)
  static const Color accentTeal = Color(0xFF00B4D8);    // Cyan / Teal (Ocean Seating pill)
  static const Color accentAmber = Color(0xFFD97706);   // Warm Amber / Yellow

  // Neutral Backgrounds
  static const Color background = Color(0xFFF4F6F8);    // Screen off-white background
  static const Color surface = Colors.white;            // Card surface
  static const Color border = Color(0xFFE2E8F0);         // Soft border line

  // KPI Tint Backgrounds
  static const Color mintTint = Color(0xFFECF7ED);      // Bookings tint
  static const Color peachTint = Color(0xFFFDF0ED);     // Turnover tint
  static const Color amberTint = Color(0xFFFFF8E7);     // Queue wait tint
  static const Color skyTint = Color(0xFFE0F2FE);       // Active tables tint

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);   // Main dark text
  static const Color textSecondary = Color(0xFF475569); // Subtitle grey text
  static const Color textMuted = Color(0xFF94A3B8);     // Light muted grey text

  // Status Colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
}
